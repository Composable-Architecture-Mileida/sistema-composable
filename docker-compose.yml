services:
  frontend:
    build: .
    container_name: sistema-composable-frontend
    ports:
      - "4200:4200"
    environment:
      - NODE_ENV=production
    restart: always

# Aquí agregaremos el servicio 'backend' más adelante